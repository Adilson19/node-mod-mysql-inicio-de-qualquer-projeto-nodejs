<!--            Criando uma pagina totalmente Estatica-->
{{>partials/header}}   
        Opa  {{ name }} {{ lastName }}, tudo bem?
        {{#showOld}}
            Como vai a vida?
        {{/showOld}}
        
        {{^showOld}}
            Beleza?
        {{/showOld}}

        <hr/>

        <!--<fieldset>
            <legend>Novo Usuário</legend>
            <form method="POST" action="/novoUsuario">  
                <input type="text" name="name" placeholder="Digite um nome"/><br/><br/>
                <input type="text" name="age" placeholder="Digite uma idade"/><br/><br/>
                <input type="submit" value="Inserir Usuário"/>
            </form>
        </fieldset>-->

        <h2>Usuários do banco</h2>
        <ul>
            {{#users}}
                <li>{{name}} (Primeira letra: {{firstLetterOfName}})
                - Idade: {{age}} 
                - <a href="/usuario/{{id}}/mais">+</a>
                - <a href="/usuario/{{id}}/menos">-</a>
                - <a href="/usuario/{{id}}/excluir">excluir</a>
                </li>
            {{/users}}
        </ul>


        <hr/>
        <h2>Produtos</h2>
        <ul>
            <!-- Exibindo duas variaveis loop -->
            {{#products}}
                {{>partials/productItem}}
            {{/products}}
        </ul>

        <hr/>
        <h2>Produtos a partir de R$ 12</h2>
        <ul>
            <!-- Exibindo duas variaveis loop -->
            {{#expensives}}
                {{>partials/productItem}}
            {{/expensives}}
        </ul>

        <hr/>

        <ul>
            <!--Quando nao tem variaveis-->
            {{#frasesDoDia}}
                <li>{{.}}</li>
            {{/frasesDoDia}}
            <!-- Como se nao houvesse frase - ELSE -->
            {{^frasesDoDia}}
                Não há frases motivacionais hoje.
            {{/frasesDoDia}}
        </ul>

{{>partials/footer}}
